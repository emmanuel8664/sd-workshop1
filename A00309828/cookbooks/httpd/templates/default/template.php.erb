<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>EJEMPLO</title>
  <style media="screen">
  h1 {
    font-size: 35px;
    text-align: center;
    margin-top: 100px;
    padding-left: 30px;
  }
  .color{
    color: #1d9fe8;
    font-weight: bold;
  }
  </style>
</head>
<body>
  <?php
  $con = mysql_connect("192.168.56.102","<%= @service_name %>","<%= @service_name %>");
  if (!$con)
  {
    die('ERROR: ' . mysql_error() . "\n---" . mysql_errno());
  }
  mysql_select_db("database1", $con);

  $result = mysql_query("SELECT * FROM WebServer WHERE name='<%= @service_name %>'");

  while ($fila = mysql_fetch_assoc($result))
  {
    echo "<h1>Solicitud atendida por el servidor web <span class='color'>" . $fila["name"] . "</span> con direccion ip <span class='color'>" . $fila["ip"] . "</span></h1>";
  }
  mysql_close($con);
  ?>
</body>
</html>
